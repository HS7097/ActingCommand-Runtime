# SPDX-License-Identifier: AGPL-3.0-only

[package]
name = "actingledger"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[[bin]]
name = "actingledger"
path = "src/main.rs"

[dependencies]
actingcommand-ledger-forensics = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
actingcommand-artifact-store = { path = "../../crates/artifact-store", features = ["capture"] }
actingcommand-contract = { workspace = true }
actingcommand-ledger = { workspace = true }
tempfile = "3"
